<%= form_for(@historial_estado_pedido) do |f| %>
  <% if @historial_estado_pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@historial_estado_pedido.errors.count, "error") %> prohibited this historial_estado_pedido from being saved:</h2>

      <ul>
      <% @historial_estado_pedido.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ped_cod %><br>
    <%= f.number_field :ped_cod %>
  </div>
  <div class="field">
    <%= f.label :estado_ped_cod %><br>
    <%= f.number_field :estado_ped_cod %>
  </div>
  <div class="field">
    <%= f.label :hist_est_ped_cod %><br>
    <%= f.number_field :hist_est_ped_cod %>
  </div>
  <div class="field">
    <%= f.label :hist_est_ped_fecha %><br>
    <%= f.date_select :hist_est_ped_fecha %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
